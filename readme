>npm init
>npm install express morgan cors dotenv mysql2 bcrypt nunjucks chokidar
//github repository 에는 node_modules 는 올라가지 않음
//팀원이 clone 하면 package.json 만 올라간다
//package.json에 명시된 모듈들이 다운로드 된다. 팀장과 동일 환경이 된다.

//서버 구동
>npx nodemon app.js


-------------------------------------------------------------------
front_end
>npm init vite skeleton_front -- --template react
>cd skeleton_front
>npm install
>npm install axios bootstrap react-router react-router-dom styled-component

//front 
>npm run dev

public/js/a.js
-component 에서 import 해서 사용되는 js 가 아니라
-html 파일에 <script> 로 이용되는 파일..

vendor - 특별한 의미의 단어가 아니라.
bootstrap template 을 제공하는 개인/ 기관등에서 만든 무언가

------------------------------------------------------------------------
git
//개별 github repository 에 push
>git add .
>git commit -m "aaaa"
>git push -u origin main